apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ingressroute-yosoyapp
  namespace: apps
spec:
  entryPoints:
    - web
  routes:
  - match: Host(`app.yo-soy.co`)
    kind: Rule
    services:
    - name: yosoyapp
      port: 80

---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ingressroute-yosoyapp-secure
  namespace: apps
spec:
  entryPoints:
    - websecure
  routes:
  - match: Host(`app.yo-soy.co`)
    kind: Rule
    services:
    - name: yosoyapp
      port: 80
  tls:
    certResolver: letsencrypt
