apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: yosoyapp
  namespace: apps
  labels:
    app: yosoyapp
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: yosoyapp
  minReplicas: 1
  maxReplicas: 10
  targetCPUUtilizationPercentage: 75
